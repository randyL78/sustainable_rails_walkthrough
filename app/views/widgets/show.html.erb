<div class="pa3">
  <h1>Widget Information</h1>
  <h2 data-testid="widget-name"><%= @widget_presenter.name %></h2>
  <h2>ID #<%= styled_widget_id(@widget_presenter.user_facing_identifier) %></h2>

  <% if flash[:notice].present? %>
    <aside>
      <%= flash[:notice] %>
    </aside>
  <% end %>
  <h3>
    Built by <%= @widget_presenter.manufacturer.name %>
  </h3>

  <%= widget_rating_component(@widget_presenter) %>
</div>
